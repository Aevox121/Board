{
  "name": "@board/server",
  "version": "0.0.0",
  "private": true,
  "description": "Board 本地服务：文件监听、双向同步、HTTP/WS API、MCP Server",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@board/core": "workspace:*",
    "chokidar": "^3.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0"
  }
}
